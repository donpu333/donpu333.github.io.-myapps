let userAlerts = [];
let currentAlertFilter = 'active';

function loadUserAlerts(filter = 'active') {
    // Загрузка и отображение алертов
}

function addUserAlert(symbol, type, condition, value, notificationMethods, notificationCount, chatId) {
    // Добавление нового алерта
}

function checkAlerts() {
    // Проверка срабатывания алертов
}

// Другие функции для работы с алертами...
